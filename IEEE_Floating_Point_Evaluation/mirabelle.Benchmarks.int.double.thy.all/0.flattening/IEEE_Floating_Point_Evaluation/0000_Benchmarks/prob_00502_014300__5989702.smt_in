; As of now it is a bit unclear how exactly a correct SMT-LIB 3 model is formatted.
; Hence, this will be corrected once the standard is released.
; No options
(import (FloatingPoint, Reals, Ints))
(open (FloatingPoint, Reals, Ints))
(declare-const y$ (FloatingPoint 11 53))
(declare-const x$ (FloatingPoint 11 53))
(assert (! (not (fp.eq (fp.mul RTN x$ y$) (fp.neg (fp.mul RTP (fp.neg x$) y$)))) :named conjecture0))
(assert (! (not (fp.isNaN x$)) :named hypothesis1))
(assert (! (not (fp.isNaN y$)) :named hypothesis2))
(check-sat)
(get-proof)
