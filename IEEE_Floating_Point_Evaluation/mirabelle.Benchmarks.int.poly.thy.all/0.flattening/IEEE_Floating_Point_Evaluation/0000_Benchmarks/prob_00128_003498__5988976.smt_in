; As of now it is a bit unclear how exactly a correct SMT-LIB 3 model is formatted.
; Hence, this will be corrected once the standard is released.
; No options
(import (FloatingPoint, Reals, Ints))
(open (FloatingPoint, Reals, Ints))
(declare-type E_f_floatSingleNaN_set$ ())
(declare-type E_f_floatSingleNaN_bool_fun$ ())
(declare-const is_finite$ E_f_floatSingleNaN_bool_fun$)
(declare-const bot$ E_f_floatSingleNaN_set$)
(declare-const collect$ (-> E_f_floatSingleNaN_bool_fun$ E_f_floatSingleNaN_set$))
(assert (! (not (not (= (collect$ is_finite$) bot$))) :named conjecture0))
(check-sat)
(get-proof)
